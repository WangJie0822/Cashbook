<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 应用主题 -->
    <style name="Theme.Cashbook" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 主要颜色 -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/color_on_primary</item>
        <!-- 次要颜色 -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/color_on_secondary</item>
        <!-- 正常控制颜色 -->
        <item name="colorControlNormal">@color/icons</item>
        <!-- 状态栏颜色 -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>

        <!-- 窗口背景 -->
        <item name="android:windowBackground">@color/white</item>

        <!-- 动画相关 -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
    </style>

    <!-- 闪屏界面主题 -->
    <style name="Theme.Cashbook.Splash">
        <!-- 窗口背景 -->
        <item name="android:windowBackground">@drawable/layer_splash_bg</item>
    </style>

    <style name="Theme.Cashbook.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Cashbook.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>